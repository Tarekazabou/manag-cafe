name: coffee_shop_manager
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  provider: ^6.1.2
  font_awesome_flutter: ^10.6.0
  animations: ^2.0.8
  sqflite: ^2.3.3+1
  path: ^1.8.3
  csv: ^6.0.0
  share_plus: ^11.0.0
  path_provider: ^2.1.1
  file_picker: ^10.1.2  # Update to the latest version
  fl_chart: ^0.71.0
  connectivity_plus: ^5.0.2
  firebase_auth: ^4.16.0
  firebase_messaging: ^14.7.10
  logger: ^2.0.2
  intl: ^0.19.0
  uuid: ^4.0.0
  firebase_core: ^2.24.0
  firebase_database: ^10.3.0
  cloud_firestore: ^4.14.0  
  
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  generate: true